"""
For example factorize(18) should return [2, 3, 3] 
and factorize(23) should return [23], because 23 is 
a prime.

Use this to test your implementation:

"""

def factorize(n: int) -> list:
    fact_list = []


def test_factorize():
    assert factorize(412415) == [5, 82483]
    assert factorize(27) == [3, 3, 3]
    assert factorize(31) == [31]

